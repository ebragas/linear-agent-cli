{
  "name": "@ebragas/linear-cli",
  "version": "0.1.0",
  "description": "CLI tool for AI agents to interact with Linear",
  "main": "./dist/cli.js",
  "bin": {
    "linear": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@linear/sdk": "^37.0.0",
    "commander": "^13.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.4",
    "tsup": "^8.4.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
